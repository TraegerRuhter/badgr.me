{
  "name": "@alarmed/crypto",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"(no lint configured for crypto)\""
  },
  "dependencies": {
    "@alarmed/core": "workspace:*",
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
